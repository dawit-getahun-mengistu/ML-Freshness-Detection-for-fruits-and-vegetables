The documentation is in the PDF file. The flutter application is inside the "application" folder and it should work for any android device with sdk 33. And the flutter code is found in the "flutter implementation" folder. The final model we used for the application along with the jupyter-notebooks we used and different performance graphycs is also provided. 

The links to the datasets we used is in the other text file. 